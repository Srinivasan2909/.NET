@model IEnumerable<onlinelist.Models.cart>

@{
    ViewBag.Title = "Displayfinal";
}

<h2>Displayfinal</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.book_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.author_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.price)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.booklist.book_name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.genre.genre_name)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.book_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.author_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.price)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.booklist.book_name)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.genre.genre_name)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.serial_no }) |
            @Html.ActionLink("Details", "Details", new { id=item.serial_no }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.serial_no })
        </td>
    </tr>
}

</table>
